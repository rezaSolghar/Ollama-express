export const RandomMathProblemCreator = (subject: string) => {
    return `create a mathematic question about ${subject} the question starts with question:
          , with 4 answers(   A), B), C), D)   ) that one of them is correct and no need to return the correct answer.`
} 

